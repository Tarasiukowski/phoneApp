export type props = {
  mini?: boolean;
  big?: boolean;
  size?: string;
  fontSize?: string;
  margin?: string;
  friend?: {
    firstname: string;
    lastname: string;
  };
  colorImage?: string
};
