import { absolute } from "../../../interfaces"

export type props = {
  disabled?: boolean;
  transparent?: boolean;
  absolute?: absolute;
  width?: string;
  margin?: string;
  alert?: boolean;
  waring?: boolean;
};
